/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./app/**/*.{js,ts,jsx,tsx}",
      // 필요하다면 pages, components 폴더 등도 추가
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };
  